mutation LoginWithGoogle($token: String!) {
    loginWithGoogle(token: $token) {
        message
        success
        user {
            email
            name
            photoUrl
            userId
        }
    }
}

mutation LogoutWithGoogle {
    logout
}