import { describe, expect, it } from "vitest";
import { UserAtom } from "./UserAtom";

describe("UserAtom", () => {
    it("should be defined", () => {
        expect(UserAtom).toBeDefined();
    });
});
